/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: ['x3q6cp-3000.csb.app']
        }
    }
};

module.exports = nextConfig;
